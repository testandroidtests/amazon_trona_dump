<?xml version="1.0" encoding="utf-8"?>
<permissions>
    <!-- By declaring the feature amazon.hardware.voicedsp here
         The api PackageManager.hasSystemFeature(String feature) will
         return true if passed the String "amazon.hardware.voicedsp".-->
    <feature name="amazon.hardware.voicedsp" />
    <!-- By declaring the feature com.amazon.hardware.voicedsp here
         The api PackageManager.hasSystemFeature(String feature) will
         return true if passed the String "com.amazon.hardware.voicedsp".-->
    <feature name="com.amazon.hardware.voicedsp" />
</permissions>
